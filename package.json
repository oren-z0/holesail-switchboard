{
  "name": "holesail-switchboard",
  "type": "commonjs",
  "version": "2.0.3",
  "description": "A web interface to manage multiple holesail servers and clients",
  "main": "dist/server.js",
  "bin": {
    "holesail-switchboard": "dist/server.js"
  },
  "files": [
    "dist",
    ".env.example",
    "README.md",
    "LICENSE"
  ],
  "scripts": {
    "start": "node -r dotenv/config dist/server.js",
    "ensure-dist": "mkdir -p dist",
    "dev": "npm run clean && concurrently \"npm run watch:server\" \"npm run watch:css\" \"npm run watch:static\"",
    "watch:server": "npm run ensure-dist && nodemon -w \"src/*.js\" -w \".env\" --exec \"npm run build:server && node -r dotenv/config dist/server.js\"",
    "watch:css": "npm run ensure-dist && npm run build:css -- --watch",
    "watch:static": "npm run ensure-dist && nodemon -w \"src/static/**/*\" -e \"*\" --exec \"npm run build:static\"",
    "build": "npm run build:server && npm run build:static && npm run build:css",
    "build:server": "npm run ensure-dist && cp src/*.js dist/",
    "build:static": "npm run ensure-dist && mkdir -p dist/static && cp -r src/static/* dist/static && cp node_modules/qr-code-styling/lib/qr-code-styling.js dist/static/qr-code-styling.js && cp node_modules/alpinejs/dist/cdn.min.js dist/static/alpine.js",
    "build:css": "npm run ensure-dist && tailwindcss -i ./src/css/style.css -o ./dist/static/style.css",
    "docker:build-to-registry": "docker buildx build --platform linux/arm64,linux/amd64 --progress plain --tag orenz0/holesail-switchboard:v$(node -p \"require('./package.json').version\") . --output type=registry",
    "lint": "eslint .",
    "lint:fix": "eslint . --fix",
    "clean": "rm -rf dist"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/oren-z0/holesail-switchboard.git"
  },
  "dependencies": {
    "@fastify/jwt": "^9.1.0",
    "@fastify/static": "^8.3.0",
    "commander": "^14.0.3",
    "fastify": "^5.7.2",
    "holesail": "^2.4.1",
    "jsonwebtoken": "^9.0.3",
    "open": "^11.0.0",
    "p-limit": "^7.2.0"
  },
  "devDependencies": {
    "@eslint/js": "^9.21.0",
    "@tailwindcss/cli": "^4.1.18",
    "alpinejs": "^3.15.6",
    "concurrently": "^9.2.1",
    "dotenv": "^17.2.3",
    "eslint": "^9.39.2",
    "eslint-plugin-html": "^8.1.4",
    "eslint-plugin-promise": "^7.2.1",
    "nodemon": "^3.1.11",
    "qr-code-styling": "^1.9.2",
    "tailwindcss": "^4.0.9"
  },
  "keywords": [
    "Holesail"
  ],
  "author": "orenz0@protonmail.com",
  "license": "AGPL-3.0",
  "bugs": {
    "url": "https://github.com/oren-z0/holesail-switchboard/issues"
  },
  "homepage": "https://github.com/oren-z0/holesail-switchboard#readme"
}
